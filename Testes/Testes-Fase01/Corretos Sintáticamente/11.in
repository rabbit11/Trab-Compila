function somaD (c : Int, d : Int) -> Int {
  return c + d;
}

function somaP (a : Int, B : Int) -> Int {
  var x : Int;
  x = 1;

  while x != 4 {
    a = somaD (a,b);
    x = x + 1;
  }
}

function main{
  writeln (somaP (1,2));
}
